{
  "name": "ha-connector-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "HostAfrica Connector — one connector for every AI coding workflow, built on the HostAfrica remote MCP server",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/cli && npm run build -w packages/vscode-extension",
    "check": "npm run build -w packages/core && npm run check --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "clean": "rm -rf packages/*/dist packages/*/dist-test"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
